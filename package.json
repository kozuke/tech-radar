{
  "name": "tech-radar",
  "version": "1.0.0",
  "private": true,
  "description": "Tech Radar - 技術トレンド閲覧サイト",
  "type": "module",
  "scripts": {
    "prepare-site": "node scripts/prepare-site-data.mjs",
    "dev": "npm run prepare-site && vitepress dev site",
    "build": "npm run prepare-site && vitepress build site",
    "preview": "npm run prepare-site && vitepress preview site"
  },
  "devDependencies": {
    "vitepress": "^1.5.0",
    "vue": "^3.5.0"
  }
}
